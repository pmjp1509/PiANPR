# ANPR pipeline -- Raspberry Pi 4 Model B (4GB), Python 3.9+
#
# Core (always needed)
numpy
opencv-python            # use opencv-python-headless on a headless Pi (SHOW_WINDOW=False)
onnxruntime              # CPU build; runs both YOLO and RapidOCR's models
rapidocr                 # PP-OCR recognition on onnxruntime (NO paddlepaddle)

# Optional -- only if settings.USE_BYTETRACK = True
supervision

# Install:
#   pip install -r requirements.txt
#
# Reclaim SD-card space by removing the broken/heavy stack (segfaults on aarch64):
#   pip uninstall paddlepaddle paddlex paddleocr
